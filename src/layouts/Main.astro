<div class='headerEmpty'></div>

<div class='container'>
  <main>
    <slot />
  </main>
</div>

<style>
  .headerEmpty {
    height: var(--header-height);
  }
  main {
    margin: var(--aside-width);
    max-width: clamp(640px, 100vw, 1280px);
  }
</style>
